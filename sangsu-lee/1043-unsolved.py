class Solution:
    def maxSumAfterPartitioning(self, A: List[int], K: int) -> int:
        mx = 0
        for i in range(1, min(len(A), K+1)):
           x = self.cut() 
        pass
    def cut(self, cum, arr):

        
